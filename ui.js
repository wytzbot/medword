export function showToast(msg){const x=document.createElement('div');x.className='toast';x.textContent=msg;document.body.appendChild(x);setTimeout(()=>x.remove(),2200)}
export async function celebrate(level,total){return new Promise(resolve=>{const c=document.createElement('div');c.className='celebrate';const badge=Number(total)>0&&Number(total)%10===0;const bits=Array.from({length:45},(_,i)=>`<i style="left:${Math.random()*100}%;top:-${Math.random()*30}px;animation-delay:${Math.random()*.8}s"></i>`).join('');c.innerHTML=`<div class="confetti">${bits}</div><div class="modal"><div class="badge">${badge?'🏆':'🎉'}</div><h2>${badge?'Congratulations!':'Level Complete!'}</h2><p>${badge?`You completed ${total} levels!`:`Great work — level ${level} is complete.`}</p>${badge?'<p class="muted">New badge unlocked.</p>':''}<button class="btn" id="celebrateOk">Continue</button></div>`;document.body.appendChild(c);c.querySelector('#celebrateOk').onclick=()=>{c.remove();resolve()}})}
export function sheet(html){return new Promise(resolve=>{const s=document.createElement('div');s.className='sheet';s.innerHTML=`<div class="modal">${html}<button class="btn ghost" id="cancel" type="button">Cancel</button></div>`;document.body.appendChild(s);s.querySelector('#cancel').onclick=()=>{s.remove();resolve(null)};s.querySelector('#pay')?.addEventListener('click',()=>{const e=s.querySelector('#email')?.value.trim();if(!e||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e))return showToast('Enter a valid email');s.remove();resolve(e)})})}
